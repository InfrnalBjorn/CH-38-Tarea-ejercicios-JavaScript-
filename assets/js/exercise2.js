function numerosDuplicar(numeros) {
  const numerosDuplicados = numeros.map((numerosEntrada) => numerosEntrada * 2);
  console.log(numerosEntrada);
  console.log(numerosDuplicados);
}

const numerosEntrada = [1, 2, 4, 5];
numerosDuplicar(numerosEntrada);
